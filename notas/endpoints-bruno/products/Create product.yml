info:
  name: Create product
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:3000/products/seed
  body:
    type: form-urlencoded
    data:
      - name: name
        value: carta
      - name: price
        value: "99"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
