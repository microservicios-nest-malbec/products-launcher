info:
  name: seed
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/products/seed
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
