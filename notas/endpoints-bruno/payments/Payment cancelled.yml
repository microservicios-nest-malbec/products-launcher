info:
  name: Payment cancelled
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3003/payments/cancel
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
