info:
  name: Payment success
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3003/payments/success
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
