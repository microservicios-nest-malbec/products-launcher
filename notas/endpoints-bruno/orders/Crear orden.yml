info:
  name: Crear orden
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/orders
  body:
    type: json
    data: |-
      {
          "items": [
              {
                "productId": 6,
                "price": 100,
                "quantity": 2  
              },
              {
                "productId": 7,
                "price": 40,
                "quantity": 10  
              }
          ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
